<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.comicverse">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComicVerse"
        tools:targetApi="31">
        <activity
            android:name=".Books_Available.Volume"
            android:exported="false"
            android:label="@string/title_activity_volume"
            android:theme="@style/Theme.ComicVerse" />
        <activity
            android:name=".Books_Available.Books"
            android:exported="false"
            android:label="@string/title_activity_books"
            android:theme="@style/Theme.ComicVerse" />
        <activity
            android:name=".Books_Available.VolumeActivity"
            android:exported="false"
            android:label="@string/title_activity_volume"
            android:theme="@style/Theme.ComicVerse" />
        <activity
            android:name=".Books_Available.BookActivity"
            android:exported="false"
            android:label="@string/title_activity_book"
            android:theme="@style/Theme.ComicVerse" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ComicVerse">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>